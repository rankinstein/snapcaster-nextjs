/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // images
  images: {
    domains: ['crystalcommerce.com',
    'binderpos.com'
  ],
  },
}

module.exports = nextConfig
